version: "3.8"

services:
  api:
    build: .
    container_name: recommender_server
    restart: unless-stopped
    env_file:
      - .env
      
    volumes:
      - ./app/models:/code/app/models

    ports:
      - "8000:8000"
